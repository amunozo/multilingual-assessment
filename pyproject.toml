[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = ["external", "vendor"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
